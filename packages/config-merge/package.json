{
    "name": "config-merge",
    "version": "1.0.0",
    "description": "Generic TypeScript library to merge a local environment config with a remote JSON configuration.",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "test": "jest --coverage"
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "jest": "^30.2.0",
        "jest-environment-jsdom": "^30.2.0",
        "ts-jest": "^29.4.6",
        "typescript": "^5.4.0"
    }
}